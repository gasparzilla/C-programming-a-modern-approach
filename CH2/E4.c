#include <stdio.h>

int main(void) {
    int i1, i2, i3, i4;
    float f1, f2, f3, f4;

    prinf("integers: %d, %d, %d, %d", i1, i2, i3, i4);
    prinf("floats: %.2f, %.2f, %.2f, %.2f", f1, f2, f3, f4);

    return 0;
}