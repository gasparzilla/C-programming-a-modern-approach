#include <stdio.h>

int main(void) {
    int i1, i2, i3, i4;
    float f1, f2, f3, f4;

    printf("integers: %d, %d, %d, %d\n", i1, i2, i3, i4);
    printf("floats: %.2f, %.2f, %.2f, %.2f\n", f1, f2, f3, f4);

    return 0;
}